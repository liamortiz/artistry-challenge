<h2>Artist#Index</h2>
<ul>
  <% @artists.each do |artist| %>
    <h3><%= link_to artist.name, artist_path(artist) %></h3>
    <p>Title: <%= artist.title %></p>
    <p>Age: <%= artist.age %></p>
  <% end %>
</ul>
